# Endpoint Status Checker v3 Residual Findings

This review covers v3 production code after Tickets 1-4 and post-reconciliation PRs #57, #58, and #59 were merged. The current v3 integration branch is `Main-Dev-V3`.

The codebase is not fully untangled. The programme reduced several compatibility-sensitive boundaries, but large WinForms classes still contain protocol, persistence, export, and UI logic that should be changed only with stronger characterization.

| File and symbol | Remaining issue | Reason it was not changed | Severity | Recommended future action |
| --- | --- | --- | --- | --- |
| `src/CheckerMainForm.cs` / `CheckerMainForm` | The class remains very large and still coordinates UI, endpoint checks, HTTP/FTP/ping/DNS/MAC work, persistence, export, theming, and tray behavior. | Splitting the whole form safely requires broader UI and workflow characterization than is available in this environment. | High | Continue extracting cohesive boundaries one protocol or workflow at a time, keeping the form as orchestration. |
| `src/CheckerMainForm.cs` / `bw_GetStatus_DoWork` | The endpoint-check workflow still has deeply nested protocol, redirect, response, SSL, DNS, MAC, cancellation, and export orchestration logic. Deterministic seams now cover HTTP request/retry/redirect/response-interpretation, HTTP response-body stream/meta gating, HTML metadata parsing, Cloudflare bypass invocation/result/error interpretation, SSL certificate acquisition/property mapping, network-share acquisition/enumeration/result shaping, ping retry, FTP request setup, FTP status/exception mapping, network identity shaping, and terminal finalization, while orchestration remains centralized in the form. | Remaining risk is now mostly orchestration density and UI/background-worker sequencing sensitivity rather than missing protocol seams. | High | Keep `CheckerMainForm` as intentional orchestration for compatibility and move next extraction focus to async safety in `EndpointDetailsDialog` and `Application.DoEvents` reduction paths. |
| `src/CheckerMainForm.cs` / `EndpointsStatusExport` | HTML post-processing and summary hyperlink replacement are extracted (`EndpointHtmlExportTransformer`), and XLSX workbook assembly is extracted (`EndpointXlsxExportWorkbookBuilder`), but overall export orchestration still remains in the form and still combines Spire.Xls conversion, file locking, progress UI, and `Application.DoEvents`. | Workbook/HTML compatibility characterization is now complete, but orchestration still couples UI timing and multi-format file I/O behavior. | High | Continue export decomposition by extracting Spire HTML conversion and file-lock orchestration seams in small tickets while preserving current file outputs and lock lifecycle. |
| `src/CheckerMainForm.cs` / comments throughout the form | Many comments repeat the following statement or compensate for method size, such as section labels inside large workflows. | Removing comments in bulk would create noisy churn and could discard useful compatibility hints mixed among redundant comments. | Medium | Remove redundant comments opportunistically when the surrounding method is extracted or reduced. |
| `src/EndpointDetailsDialog.cs` / `EndpointDetailsDialog` | Dialog still mixes UI presentation with favicon lookup, WMI, port checks, WHOIS, MAC vendor lookup, geolocation, VirusTotal, page-link validation, and process launching. | Each workflow has external I/O and user-visible behavior; safe extraction needs focused tests and potentially separate tickets. | High | Extract network/API clients and result mappers behind narrow internal boundaries, starting with pure formatting/mapping logic. |
| `src/SpeedTestDialog.cs` / `SpeedTestDialog` | Dialog remains large and combines UI state, speed-test orchestration, calculations, rendering, settings, and exception presentation. | Speed-test behavior depends on external servers and UI timing, which are not safely covered by existing tests. | Medium | Characterize calculation and formatting helpers first, then isolate speed-test orchestration from UI updates. |
| `src/Program.cs` / global static application state | Program owns mutable global configuration, file names, API keys, app metadata, and startup/update flow. | Several public/static members are effectively public compatibility surface and are used across forms. | Medium | Introduce read-only snapshots for new boundaries, but do not remove or rename public/static fields without an explicit compatibility decision. |
| `src/CloudflareBypassChecker.cs` / bypass methods | Cloudflare bypass logic mixes process management, FlareSolverr HTTP calls, Playwright/CDP websocket control, JSON parsing, and cleanup. | Behavior is protocol- and environment-sensitive; changing it risks breaking configured bypass workflows. | High | Add tests around result mapping and command construction, then split FlareSolverr and Playwright implementations. |
| `src/ExceptionDialog.cs` and `src/FeatureRequestDialog.cs` / mail composition and fallback save | HTML table composition duplication was reduced via `ReportMailTableBuilder`, but `.eml` save, temp directory handling, and user-visible status/exception flows remain duplicated. | Public/user-facing error reporting must be preserved; broader consolidation still needs characterization of save/open/error paths. | Medium | Add deterministic tests around `.eml` generation path and fallback/save error messaging before extracting additional shared mail-send orchestration. |
| `src/CheckerMainForm.cs`, `src/EndpointDetailsDialog.cs`, `src/FeatureRequestDialog.cs`, `src/ExceptionDialog.cs` / `NewBackgroundThread` and `ThreadSafeInvoke` | Wrapper implementations were consolidated through `UiThreadHelpers`, but legacy `Application.DoEvents` usage remains in wrapper call paths and in other long-running workflows. | Consolidation removed duplication but did not de-risk reentrancy/timing side effects from `DoEvents`. | Medium | Characterize `DoEvents`-sensitive sequencing and then reduce unsafe `DoEvents` usage incrementally, preserving user-visible behavior. |
| `src/EndpointDetailsDialog.cs` / VirusTotal task blocking and polling flow | VirusTotal scan enqueue and report polling now both use deterministic cancellation/transition seams (`EndpointVirusTotalScanRetryExecutor`, `EndpointVirusTotalCancellationGate`, `EndpointVirusTotalReportWorkflow`), while worker orchestration still intentionally uses synchronous bridge completion for compatibility. | Full async conversion is compatibility-sensitive because timing, status text sequencing, and tab-removal behavior are user-visible. | Medium | Keep current seam-based synchronous orchestration for compatibility and continue characterizing remaining dialog workflows with the same deterministic approach. |
| `src/EndpointDetailsDialog.cs` / MAC-vendor and favicon workflows | Recursive favicon retry was removed and deterministic planning/cancellation/result-shaping seams were added, but remaining image/link-validation workflows still use broad exception swallowing and incomplete lifecycle characterization. | Multiple network-dependent branches are still coupled to dialog orchestration and require additional cancellation/transition coverage work. | Medium | Continue Priority 4 with link-validation and timer/closing lifecycle seams, then perform an independent closure review over all dialog workflows. |
| `src/ArpLookup/WindowsLookupService.cs` / `Lookup` | ARP lookup uses platform-specific command/PInvoke behavior and a static throttle. | It is compatibility-sensitive for MAC resolution and Windows networking behavior. | Medium | Keep isolated; add tests around formatting and failure mapping before any behavioral cleanup. |
| `src/IPAddressExtensions.cs` / namespace `Knom.Helpers.Net` | Namespace and helper naming do not match the application namespace. | Public extension methods may be referenced externally; renaming would be a public API change. | Low | Keep public namespace unless a compatibility-breaking cleanup is explicitly approved. |

See `docs/refactoring/reconciliation-2026-08-04.md` for the strict completion matrix across reconciled priorities.
